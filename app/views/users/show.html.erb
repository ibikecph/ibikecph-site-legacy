<%= link_to "Edit", [:edit,:account], :class => "button righty" if current_user == @user %>
<h1><%= @user.name %><%= ' (you)' if current_user == @user %></h1>

<p><%= image_tag @user.image.g4.url, :class => 'g4 square' %></p>

<% if @user.about %>
	<%= simple_format(@user.about) %>
<% end %>
