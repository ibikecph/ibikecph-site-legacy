<div class="gr">
	<div class="g4">
		<%= render 'themes/themes' %>
		<%= render 'issues/most_commented' %>
		<%= render 'issues/tags' %>	</div>
	<div class="g8 ge">
		<%= render @theme %>
		<br>
		<%= render :partial => 'issues/issues', :locals => { :title => t('.issues'), :new_url => new_issue_for_theme_path } %>
	</div>
	
</div>
